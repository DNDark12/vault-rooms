node_modules/
.pnpm-store/

data/
docs/
coverage/
dist/
apps/*/dist/
packages/*/dist/
*.tsbuildinfo

.env
.env.*
!.env.example

*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

release/
*.zip

.DS_Store

# Stray tsc output that once leaked into apps/relay-server/test/ (source is health.test.ts).
# Safe to delete manually; kept ignored here in case it reappears from a misconfigured build.
apps/relay-server/test/health.test.js
apps/relay-server/test/health.test.js.map
apps/relay-server/test/health.test.d.ts
apps/relay-server/test/health.test.d.ts.map

# Obsidian plugin build output. The root-level manifest.json/main.js/styles.css (kept in sync via
# `pnpm build:plugin`) are the committed, installable copies - see "Installing the Obsidian plugin
# manually" and "Release checklist" in README.md. sql-wasm.wasm is embedded into main.js at build
# time (esbuild.config.mjs), not shipped as a separate file.
apps/obsidian-plugin/main.js
apps/obsidian-plugin/assets/
